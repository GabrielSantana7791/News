package com.news.gabrielSoft.util;

public enum MODEL_ATTRIBUTES {
	section, page, message, errorMessage, title, user, top3, postContent, postId, redirect,
	comments;
	//postContendo é usado para editar o post
	//postId é usado para redirecionar para um post

}
